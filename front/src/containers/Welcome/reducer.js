import { Map } from 'immutable'

const initial = Map({
})

function welcomeReducer (state = initial, action) {
  switch (action.type) {
    default:
      return state
  }
}
export default welcomeReducer
